# 교안 자동 생성 기능 — 설정 안내 (v1.0)

홈페이지에 **Dify 교안 생성 기능**을 통합했습니다. 작동하려면 Vercel에 환경변수 몇 개만 넣으면 됩니다.

---

## 1. 무엇이 추가됐나

| 파일 | 역할 |
|------|------|
| `tools/lesson-plan-7gq2m9xv.html` | **비밀 교안 생성 페이지** (이 주소를 아는 사람만 사용) |
| `api/upload.js` | 업로드한 파일을 Dify로 중개(프록시) |
| `api/generate.js` | Dify 교안 생성 워크플로우 실행 → 결과 반환 |
| `robots.txt` | 비밀 페이지·API 검색엔진 노출 차단 |

**비밀 페이지 주소** (배포 후):
```
https://yoonjungho.vercel.app/tools/lesson-plan-7gq2m9xv.html
```
> 메인 홈페이지에는 이 링크를 노출하지 않았습니다. 주소를 직접 아는 사람만 접근합니다.

---

## 2. Vercel 환경변수 설정 (필수)

Vercel 프로젝트 → **Settings → Environment Variables** 에서 아래를 추가하세요.

| 이름 | 값 | 설명 |
|------|----|----|
| `DIFY_API_KEY` | (Dify에서 발급한 키) | **필수.** Dify 워크플로우 앱의 API 키 (`app-...`) |
| `ACCESS_TOKEN` | `axtok_d3f8k1m6` | 비밀 페이지와 API를 묶는 토큰. 페이지에 적힌 값과 **같아야** 함 |
| `DIFY_API_BASE` | `https://api.dify.ai/v1` | (선택) Dify 클라우드면 생략 가능. 자체 호스팅이면 그 주소 |
| `DIFY_USER` | `web-user` | (선택) 그대로 둬도 됨 |

> **Dify API 키 찾는 곳:** Dify에서 해당 워크플로우 앱 열기 → 왼쪽 메뉴 **API 접근(API Access)** → **API 키** 발급/복사.

설정 후 Vercel에서 **재배포(Redeploy)** 해야 적용됩니다.

---

## 3. 작동 방식 (흐름)

```
사용자가 비밀 페이지에서 파일 업로드
   → /api/upload (파일 1개씩 Dify로 업로드, file_id 받음)
   → /api/generate (file_id들을 doc 변수로 워크플로우 실행)
   → Dify 가 교안(result) 반환 → 화면에 표시 + 복사 버튼
```

- Dify 워크플로우 변수 매핑: 업로드 파일 → `doc`(array[file]), 영상요약 → `url`(v1.0은 빈 값)
- 교안 결과는 Dify의 `result` 변수에서 읽습니다. (변수명이 다르면 `api/generate.js`의 `prefer` 목록만 고치면 됩니다.)

---

## 4. 알아둘 제약 (v1.0)

- **파일 크기**: 파일당 **최대 4.5MB**. Vercel 서버리스 함수의 요청 한도 때문입니다. (4.83MB짜리 보고서 등은 압축하거나 페이지를 줄여서 올려야 합니다.)
- **생성 시간**: Dify 처리에 30초~2분. Vercel **무료(Hobby) 플랜은 함수 실행 60초 제한**이라, 자료가 많아 60초를 넘으면 시간 초과가 날 수 있습니다. 그럴 땐 파일 수를 줄이거나(3~5개 권장) Vercel Pro로 올리면 됩니다.
- **저장 안 함**: 결과는 화면 표시 + 복사만. 이력 저장·PDF 다운로드는 v2.0 과제입니다.

---

## 5. 보안 참고

- 비밀 페이지 주소와 `ACCESS_TOKEN`은 **GitHub 저장소가 공개(public)면 누구나 볼 수 있습니다.** 진짜 보호가 필요하면 저장소를 **비공개(private)** 로 두거나, v2.0에서 비밀번호/로그인 방식으로 강화하세요.
- Dify API 키는 코드에 절대 적지 않았고, Vercel 환경변수로만 사용합니다. (프런트엔드 노출 없음)

---

## 6. 다음 버전(v2.0) 후보

유튜브 요약 텍스트 입력 · 슬라이드/평가지 출력 · 결과 PDF 다운로드 · 이력 저장 · 로그인 방식 접근 제한 등.
